# faq0n's custom lua utils

Place utils.lua into project folder to have access to helper functions in 'lib' 
``` 
  local utils = require("utils)
  local prompt = utils.require_local("prompt")
```

Otherwise make imports in utils.lua to load helper functions all-in-one
